import styled from 'styled-components'

export const FooterWrapper = styled.footer`

`

const Footer = () => {
    return (
        <FooterWrapper>
            Footer
        </FooterWrapper>
    )
}

export default Footer;