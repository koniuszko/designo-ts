import styled from 'styled-components'

export const HeaderWrapper = styled.header`

`

const Header = () => {
    return (
        <HeaderWrapper>
            Header
        </HeaderWrapper>
    )
}

export default Header;